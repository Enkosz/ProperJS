import ProperApplicationContext from "@core/context/proper-application-context";

describe("ProperApplicationContext", () => {

    const properApplicationContext = new ProperApplicationContext();

    test('it should succeed', () => {
        expect(0).toBe(0);
    });
})
